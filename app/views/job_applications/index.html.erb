<p id="notice"><%= notice %></p>

<h1>Listing Job Applications</h1>

<table>
  <thead>
    <tr>
      <th>Companyname</th>
      <th>Appcompleted</th>
      <th>Companyresponse</th>
      <th>Nextinterview</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_applications.each do |job_application| %>
      <tr>
        <td><%= job_application.companyName %></td>
        <td><%= job_application.appCompleted %></td>
        <td><%= job_application.companyResponse %></td>
        <td><%= job_application.nextInterview %></td>
        <td><%= link_to 'Show', job_application %></td>
        <td><%= link_to 'Edit', edit_job_application_path(job_application) %></td>
        <td><%= link_to 'Destroy', job_application, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Job application', new_job_application_path %>
